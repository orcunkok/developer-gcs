# Browser-side LLM config. The VITE_ prefix exposes these to the bundle.
# OK for local dev with your own key. Move to a server proxy before any deploy.

VITE_LLM_MODEL=openai/gpt-oss-120b
VITE_GROQ_API_KEY=
